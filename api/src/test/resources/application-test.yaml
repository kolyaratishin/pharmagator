spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://pharmagator.cluster-cglzuppzap0n.us-east-2.rds.amazonaws.com/pharmagator
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
